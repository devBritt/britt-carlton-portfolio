import { ImageList, ImageListItem, ImageListItemBar } from '@mui/material';

const Work = () => {
    return (
        <ImageList sx={{ width: 450, height: 800 }}>
            {/* map over list of projects to fill image list */}
        </ImageList>
    );
}

export default Work;
